<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>12</LangVersion>
    <Nullable>enable</Nullable>
    <Authors>Vippy</Authors>
    <!-- Fourth component = tester iteration. BepInEx rejects SemVer pre-release tags. -->
    <Version>1.7.3</Version>
    <RootNamespace>REPOFidelity</RootNamespace>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <AppendRuntimeIdentifierToOutputPath>false</AppendRuntimeIdentifierToOutputPath>
  </PropertyGroup>

  <PropertyGroup>
    <UserHome Condition="'$(UserHome)' == '' and '$(HOME)' != ''">$(HOME)</UserHome>
    <UserHome Condition="'$(UserHome)' == '' and '$(USERPROFILE)' != ''">$(USERPROFILE)</UserHome>
    <GalePluginsDir Condition="'$(GalePluginsDir)' == ''">$(UserHome)/.local/share/com.kesomannen.gale/repo/profiles/Developer/BepInEx/plugins</GalePluginsDir>
    <OutputPath>$(GalePluginsDir)/REPOFidelity/</OutputPath>
  </PropertyGroup>

  <!-- Game install path. Override with -p:RepoManagedDir=... or set REPO_MANAGED_DIR. -->
  <PropertyGroup>
    <RepoManagedDir Condition="'$(RepoManagedDir)' == '' and '$(REPO_MANAGED_DIR)' != ''">$(REPO_MANAGED_DIR)</RepoManagedDir>
    <RepoManagedDir Condition="'$(RepoManagedDir)' == '' and Exists('$(UserHome)/.local/share/Steam/steamapps/common/REPO/REPO_Data/Managed')">$(UserHome)/.local/share/Steam/steamapps/common/REPO/REPO_Data/Managed</RepoManagedDir>
    <RepoManagedDir Condition="'$(RepoManagedDir)' == '' and Exists('C:/Program Files (x86)/Steam/steamapps/common/REPO/REPO_Data/Managed')">C:/Program Files (x86)/Steam/steamapps/common/REPO/REPO_Data/Managed</RepoManagedDir>
    <RepoManagedDir Condition="'$(RepoManagedDir)' == '' and Exists('D:/SteamLibrary/steamapps/common/REPO/REPO_Data/Managed')">D:/SteamLibrary/steamapps/common/REPO/REPO_Data/Managed</RepoManagedDir>
  </PropertyGroup>

  <PropertyGroup>
    <RestoreAdditionalProjectSources>
      https://api.nuget.org/v3/index.json;
      https://nuget.bepinex.dev/v3/index.json;
      https://nuget.windows10ce.com/nuget/v3/index.json
    </RestoreAdditionalProjectSources>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="UnityShaderProject\**" />
    <None Remove="UnityShaderProject\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="BepInEx.Analyzers" Version="1.*" PrivateAssets="all" />
    <PackageReference Include="BepInEx.AssemblyPublicizer.MSBuild" Version="0.4.1" PrivateAssets="all" IncludeAssets="build" />
    <PackageReference Include="BepInEx.Core" Version="5.*" ExcludeAssets="runtime" />
    <PackageReference Include="UnityEngine.Modules" Version="2022.3.62" IncludeAssets="compile" PrivateAssets="all" />
    <Publicize Include="Assembly-CSharp" />
    <Publicize Include="UnityEngine.NVIDIAModule" />
  </ItemGroup>

  <!-- Direct game DLL references. -->
  <ItemGroup>
    <Reference Include="Assembly-CSharp">
      <HintPath>$(RepoManagedDir)/Assembly-CSharp.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Assembly-CSharp-firstpass">
      <HintPath>$(RepoManagedDir)/Assembly-CSharp-firstpass.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="PhotonUnityNetworking">
      <HintPath>$(RepoManagedDir)/PhotonUnityNetworking.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Photon3Unity3D">
      <HintPath>$(RepoManagedDir)/Photon3Unity3D.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="PhotonRealtime">
      <HintPath>$(RepoManagedDir)/PhotonRealtime.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="UnityEngine.UI">
      <HintPath>$(RepoManagedDir)/UnityEngine.UI.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Unity.Postprocessing.Runtime">
      <HintPath>$(RepoManagedDir)/Unity.Postprocessing.Runtime.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Unity.TextMeshPro">
      <HintPath>$(RepoManagedDir)/Unity.TextMeshPro.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>

  <ItemGroup>
    <Reference Include="MenuLib">
      <HintPath>$(GalePluginsDir)/nickklmao-MenuLib/MenuLib.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>

  <Target Name="GenerateBuildInfo" BeforeTargets="CoreCompile">
    <PropertyGroup>
      <BuildInfoContent>// &lt;auto-generated/&gt;
namespace REPOFidelity
{
    internal static class BuildInfo
    {
        public const string Version = "$(Version)"%3B
    }
}
</BuildInfoContent>
    </PropertyGroup>
    <WriteLinesToFile File="$(MSBuildProjectDirectory)/BuildInfo.g.cs" Lines="$(BuildInfoContent)" Overwrite="true" WriteOnlyWhenDifferent="true" />
  </Target>

  <Target Name="CopyShaderBundle" AfterTargets="Build">
    <Copy SourceFiles="$(MSBuildProjectDirectory)/repofidelity_shaders"
          DestinationFolder="$(OutputPath)"
          SkipUnchangedFiles="true"
          Condition="Exists('$(MSBuildProjectDirectory)/repofidelity_shaders')" />
  </Target>

  <UsingTask TaskName="UpdateManifestVersion" TaskFactory="RoslynCodeTaskFactory" AssemblyFile="$(MSBuildToolsPath)/Microsoft.Build.Tasks.Core.dll">
    <ParameterGroup>
      <ManifestPath ParameterType="System.String" Required="true" />
      <NewVersion ParameterType="System.String" Required="true" />
    </ParameterGroup>
    <Task>
      <Using Namespace="System.IO" />
      <Using Namespace="System.Text" />
      <Using Namespace="System.Text.RegularExpressions" />
      <Code Type="Fragment" Language="cs"><![CDATA[
        var enc = new UTF8Encoding(false);
        var content = File.ReadAllText(ManifestPath, enc);
        var updated = Regex.Replace(content, "\"version_number\"\\s*:\\s*\"[^\"]*\"", "\"version_number\": \"" + NewVersion + "\"");
        if (updated != content) File.WriteAllText(ManifestPath, updated, enc);
      ]]></Code>
    </Task>
  </UsingTask>

  <Target Name="PackageMod" AfterTargets="CopyShaderBundle">
    <PropertyGroup>
      <RepoRoot>$([System.IO.Path]::GetFullPath('$(MSBuildProjectDirectory)'))</RepoRoot>
      <BuildZipParent>$(RepoRoot)/BuildZip</BuildZipParent>
      <StageDir>$(BuildZipParent)/REPOFidelity</StageDir>
      <OutputZip>$(BuildZipParent)/REPOFidelity.zip</OutputZip>
      <SourceManifest>$(RepoRoot)/manifest.json</SourceManifest>
      <SourceIcon>$(RepoRoot)/icon.png</SourceIcon>
      <SourceReadme>$(RepoRoot)/README.md</SourceReadme>
      <SourceChangelog>$(RepoRoot)/CHANGELOG.md</SourceChangelog>
      <BuiltDll>$(OutputPath)REPOFidelity.dll</BuiltDll>
      <DlssSrc>$(OutputPath)nvngx_dlss.dll</DlssSrc>
      <ShaderBundleSrc>$(OutputPath)repofidelity_shaders</ShaderBundleSrc>
      <NgxBridgeSrc>$(OutputPath)ngx_bridge.dll</NgxBridgeSrc>
      <EnforceDlssPackaging Condition="'$(EnforceDlssPackaging)' == ''">true</EnforceDlssPackaging>
    </PropertyGroup>

    <!-- Hard gate: never ship a package without the DLSS runtime again. 1.7.1
         went out missing both ngx DLLs because these were only Warnings and the
         Copies were Exists()-guarded, so an absent file dropped silently. Fail
         the package instead of warning. For a deliberate DLSS-less build, pass
         -p:EnforceDlssPackaging=false. -->
    <Error Condition="'$(EnforceDlssPackaging)' == 'true' and !Exists('$(DlssSrc)')"
           Text="PackageMod ABORTED: nvngx_dlss.dll missing from $(OutputPath). This is the exact regression that shipped DLSS broken in 1.7.1. Restore the ngx DLLs to the build output before packaging (or pass -p:EnforceDlssPackaging=false to bypass)." />
    <Error Condition="'$(EnforceDlssPackaging)' == 'true' and !Exists('$(NgxBridgeSrc)')"
           Text="PackageMod ABORTED: ngx_bridge.dll missing from $(OutputPath). DLSS needs both ngx DLLs. Restore it before packaging (or pass -p:EnforceDlssPackaging=false to bypass)." />

    <UpdateManifestVersion ManifestPath="$(SourceManifest)" NewVersion="$(Version)" />

    <RemoveDir Directories="$(StageDir)" />
    <MakeDir Directories="$(StageDir)" />

    <Copy SourceFiles="$(BuiltDll)" DestinationFolder="$(StageDir)" />
    <Copy SourceFiles="$(SourceReadme);$(SourceChangelog);$(SourceManifest)" DestinationFolder="$(StageDir)" />
    <Copy SourceFiles="$(SourceIcon)" DestinationFolder="$(StageDir)" Condition="Exists('$(SourceIcon)')" />
    <Copy SourceFiles="$(DlssSrc)" DestinationFolder="$(StageDir)" Condition="Exists('$(DlssSrc)')" />
    <Copy SourceFiles="$(ShaderBundleSrc)" DestinationFolder="$(StageDir)" Condition="Exists('$(ShaderBundleSrc)')" />
    <Copy SourceFiles="$(NgxBridgeSrc)" DestinationFolder="$(StageDir)" Condition="Exists('$(NgxBridgeSrc)')" />

    <Warning Text="nvngx_dlss.dll not found in output - DLSS will use fallback download"
             Condition="!Exists('$(DlssSrc)')" />
    <Warning Text="icon.png not found - add before uploading"
             Condition="!Exists('$(SourceIcon)')" />

    <Delete Files="$(OutputZip)" />
    <ZipDirectory SourceDirectory="$(StageDir)" DestinationFile="$(OutputZip)" Overwrite="true" />

    <Message Importance="high" Text="[PackageMod] DLL  -> $(OutputPath)" />
    <Message Importance="high" Text="[PackageMod] Zip  -> $(OutputZip) (v$(Version))" />
  </Target>

</Project>
